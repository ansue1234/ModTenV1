# Configuration for IMU Serial Node

imu_serial_node:
  ros__parameters:
    serial_port: "/dev/ttyACM0"  # Arduino MEGA running MultiBridgeCollectionFast.ino (or its /dev/serial/by-id/... path)
    baud_rate: 115200
    timeout: 1.0
